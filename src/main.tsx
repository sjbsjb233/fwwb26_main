import React from 'react'
import ReactDOM from 'react-dom/client'
import AutoFillWebApp from './AutoFillWebApp'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AutoFillWebApp />
  </React.StrictMode>,
)
